# Versiones fijas: antes decia `Django>=5.0` y el resto sin version, asi que cada
# build de la imagen podia traer una version mayor distinta sin que nadie lo
# decidiera. Estas son las versiones contra las que corre la suite (195 tests).
#
# Para subir algo: cambiar la linea, correr `python manage.py test reports` y
# desplegar. Una a la vez.
Django==6.0.6
djangorestframework==3.17.1
psycopg2-binary==2.9.12
openpyxl==3.1.5
python-decouple==3.8
gunicorn==26.0.0
whitenoise==6.12.0
requests==2.34.2
msal==1.37.0
django-storages[google]==1.14.6
